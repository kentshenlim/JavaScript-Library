import library from './library.js';

const ui = (() => {
  // Cache DOM
  const _libraryShelf = document.getElementById('library-shelf');

  // Function declaration
  function _updateLibrary(obj) {

  }

  // Bind events
})();
